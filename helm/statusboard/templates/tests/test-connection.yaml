apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "statusboard.fullname" . }}-test-connection"
  labels:
    {{- include "statusboard.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "statusboard.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
